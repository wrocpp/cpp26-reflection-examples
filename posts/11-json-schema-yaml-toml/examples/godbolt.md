# Godbolt permalinks for post 11

Compiler Explorer hosts the `clang-p2996` fork. These permalinks pin the SHA used in this post (`9ffb96e3ce36`) plus `-std=c++26 -freflection-latest -stdlib=libc++`.

> **TODO:** Populate after first run on godbolt.org.

- [ ] **formats_demo — one annotated struct → JSON, YAML, XML** — [permalink TBD]
- [ ] **toml_format — minimal TOML policy from the post** — [permalink TBD]
