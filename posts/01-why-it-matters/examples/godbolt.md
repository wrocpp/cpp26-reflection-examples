# Godbolt permalinks for post 1

Compiler Explorer hosts the `clang-p2996` fork under its per-compiler cache. These permalinks pin the exact SHA this series uses (`9ffb96e3ce36`), plus the flags `-std=c++26 -freflection -fexpansion-statements -stdlib=libc++`.

> **TODO:** Populate after first run on godbolt.org. Each entry = one runnable example, titled.

- [ ] **teaser — 40-line JSON serializer for any aggregate** — [permalink TBD]

Regeneration is part of `scripts/check_godbolt.sh` (see repo root `README.md`).
